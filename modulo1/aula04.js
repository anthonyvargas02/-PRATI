/*SEGUNDA AULA DE PROGRAMAÇÃO*/

/*Faça um programa que solicite ao usuário a inserção de dois valor (um de cada vez) e mostre para ele duas opções:  1 - Somar ou 2 - Subtrair*/

/*const prompt = require("prompt-sync")();


let primeiroValor = Number(prompt("Digite o primeiro valor: "));
let segundoValor = Number(prompt("Digite o segundo valor: "));
let opcao = Number(prompt("1 - para somar | 2 - para substrair | ou 3 - multiplicar | 4 - Dividir : "));
//let resultado = 0

switch(opcao){
    case 1:
        console.log(primeiroValor + segundoValor);
        break;  
        
        case 2:
        console.log(primeiroValor - segundoValor);
        break;

        case 3:
            console.log(primeiroValor * segundoValor)
            break

            case 4:
                console.log(primeiroValor / segundoValor)
        
        default:
        console.log("Opção inválida");
} */

/*if (opcao === 1) {
    resultado = primeriValor + segundoValor;
} else if (opcao === 2) {
    resultado = primeriValor - segundoValor;
}
    console.log(resultado)*/ 


    /*Pergunte a iade de uma pessoa em informe se ela é jovem, adulta ou idosa */
   /* const prompt = require("prompt-sync")();
    
    let idade = Number(prompt('qual é a sua idade?'))

    switch(true){
        case (idade >= 0 && idade <= 12):
        console.log('criança')
        break

        case (idade > 12 && idade <= 18):
            console.log('Jovem')
            break

        case idade > 18:
            console.log('Adulto')
            break
    } */

            